import styled from 'styled-components';

const DonutsWrapper = styled.div`
  position: relative;
`;

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
`;

const WavesWrapper = styled.div`
  position: absolute;
  bottom: 0;
  width: 100%;
`;

export { Wrapper, DonutsWrapper, WavesWrapper };
