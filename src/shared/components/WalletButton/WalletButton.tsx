import { Button } from '@mui/material';

const WalletButton = () => {
  return (
    <Button variant="contained" color="secondary">
      Connect wallet
    </Button>
  );
};

export { WalletButton };
