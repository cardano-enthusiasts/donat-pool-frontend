import styled from 'styled-components';

const Wrapper = styled.header`
  width: 100%;
`;

const Inner = styled.div``;

const Logo = styled.div``;

const Links = styled.div``;

export { Wrapper, Inner, Logo, Links };
